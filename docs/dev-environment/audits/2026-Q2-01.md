# Auditoria 2026-Q2-01

| Campo | Valor |
|-------|-------|
| **audit_id** | 2026-Q2-01 |
| **performed_at** | 2026-04-06 |
| **performer** | Implementação assistida (build + `docker run` imagem `sandbox-dev:latest`) |
| **sample** | Inventário completo (foco P1) |
| **policy_version** | freshness-policy.md v2026-04-06 |

## Comandos de verificação

```bash
cd /caminho/para/sandbox
docker compose build dev
docker run --rm --entrypoint bash sandbox-dev:latest -lc 'export PATH="/usr/local/share/mise/installs/node/22/bin:/usr/local/share/mise/installs/php/8.4/bin:${PATH}"; php -v; node -v; nvim --version | head -1; rg --version | head -1; fd --version; tmux -V; git --version'
```

## Saída observada (2026-04-06)

```
PHP 8.4.19 (cli) ...
v22.22.2
NVIM v0.9.5
ripgrep 14.1.0
fdfind 9.0.0
tmux 3.4
git version 2.43.0
```

## Resultado

| Métrica | Valor |
|---------|-------|
| **P1 conformidade directa** | 2/3 (PHP, Node) |
| **P1 com excepção registada** | 1/3 (Neovim via distro vs upstream) |
| **% P1 ok ou excepção** | 100% (meta ≥ 90%) |

## Acções correctivas

- [ ] **nvim-editor**: decidir prazo para fechar excepção (mise Neovim ou outra fonte) — ver `platform-evaluation.md`.

## Notas

- Imagem local tag `sandbox-dev:latest` após `docker compose build dev`.  
- Node reporta `v22.22.2` via `node -v`.  
- `fd` no PATH é symlink para `fdfind` 9.0.0.
